# from keras import backend as K

from keras.applications import vgg16


class Model:
    pass
    # model = vgg16()

    # color_image_input = Input(shape=(120, 160, 3), name='color_image_input')
    # depth_image_input = Input(shape=(120, 160, 3), name='depth_image_input')
    # segmentation_image_output = Dense(shape=(120, 160, 3), name='segmentation_image_output')
    # Model(input=[])
    # model.summary()
